# Project Rules & Preferences

- **GitHub Push Instructions Requirement**: Always include clear, step-by-step terminal commands at the end of every response on how to upload/push the updated code to GitHub (`git add .`, `git commit -m "..."`, `git push origin main`).
